import { StyleSheet } from 'react-native';
import {  SECONDARY_DISABLE_GRAY } from '../../../constants/colors';
import { scale } from '../../../common/scalling';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: SECONDARY_DISABLE_GRAY,
  },
});

export default styles;
